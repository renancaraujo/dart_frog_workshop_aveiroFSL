// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';
import 'package:todos_data/todos_data.dart';

void main() {
  group('TodosData', () {
    test('can be instantiated', () {
      expect(TodosData(), isNotNull);
    });
  });
}
